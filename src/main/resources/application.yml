server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/test_database?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false